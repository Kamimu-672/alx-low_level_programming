#include "main.h"
#include <stdio.h>

/**
 * putchar - write the character c to stdout
 *
 * @c: Character to be tested
 *
 * Return: 1 if uppercase else 0 (False)
 */
int _putchar(char c)
{
	if (c >= 1 && c <= 2)			
	{
		return (1);
	}
	return (0);
}
